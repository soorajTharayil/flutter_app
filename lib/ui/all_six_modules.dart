import 'package:flutter/material.dart';

class IpFeedbackPage extends StatelessWidget {
  const IpFeedbackPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('IP Feedback')),
      body: const Center(child: Text('Welcome to IP Feedback')),
    );
  }
}

class PcfFeedbackPage extends StatelessWidget {
  const PcfFeedbackPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PCF Feedback')),
      body: const Center(child: Text('Welcome to PCF Feedback')),
    );
  }
}

class IsrFeedbackPage extends StatelessWidget {
  const IsrFeedbackPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ISR Feedback')),
      body: const Center(child: Text('Welcome to ISR Feedback')),
    );
  }
}

class IncidentReportPage extends StatelessWidget {
  const IncidentReportPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Incident Report')),
      body: const Center(child: Text('Welcome to Incident Report')),
    );
  }
}

class GrievanceFeedbackPage extends StatelessWidget {
  const GrievanceFeedbackPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Grievance Feedback')),
      body: const Center(child: Text('Welcome to Grievance Feedback')),
    );
  }
}

class IpDischargeFeedbackPage extends StatelessWidget {
  const IpDischargeFeedbackPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('IP Discharge Feedback')),
      body: const Center(child: Text('Welcome to IP Discharge Feedback')),
    );
  }
}

class IpConcernRequestPage extends StatelessWidget {
  const IpConcernRequestPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('IP Concern / Request')),
      body: const Center(child: Text('Welcome to IP Concern / Request')),
    );
  }
}

class RaiseInternalRequestPage extends StatelessWidget {
  const RaiseInternalRequestPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Raise Internal Request')),
      body: const Center(child: Text('Welcome to Raise Internal Request')),
    );
  }
}

class QualityKpiFormsPage extends StatelessWidget {
  const QualityKpiFormsPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Quality KPI Forms')),
      body: const Center(child: Text('Welcome to Quality KPI Forms')),
    );
  }
}

class HealthcareAuditFormsPage extends StatelessWidget {
  const HealthcareAuditFormsPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Healthcare Audit Forms')),
      body: const Center(child: Text('Welcome to Healthcare Audit Forms')),
    );
  }
}

class DepartmentalMonthlyReportsPage extends StatelessWidget {
  const DepartmentalMonthlyReportsPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Departmental Monthly Reports')),
      body:
          const Center(child: Text('Welcome to Departmental Monthly Reports')),
    );
  }
}

class PremFormsPage extends StatelessWidget {
  const PremFormsPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PREM Forms')),
      body: const Center(child: Text('Welcome to PREM Forms')),
    );
  }
}

class RegisterAssetPage extends StatelessWidget {
  const RegisterAssetPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Register Asset')),
      body: const Center(child: Text('Welcome to Register Asset')),
    );
  }
}
